/// Simplified stake pool module for initial deployment
/// The full implementation is available in stake_pool.move.full
/// This version removes the dependency on fungible asset objects
module stake_pool_addr::stake_pool {
    // Simplified stake pool module - temporarily disabled for initial deployment
    // To enable full staking functionality, replace this file with stake_pool.move.full
    // after creating the required fungible asset objects
}
