/// Aries bridge module - temporarily disabled for testnet compilation
/// To enable Aries integration, uncomment and ensure the Aries module is deployed at the correct address
module aptopilot::aries_bridge {
    // Aries bridge functionality will be re-enabled in the future
    // when the Aries module is available on testnet
    // See aries_bridge.move.bak for the original implementation
}
